#include <stdio.h>
#include<string.h>
int main(void) 
{
	char a[50];
	int i,k;
	scanf("%s",a);
	k=strlen(a);
	for(i=0;i<k;i++)
	{
		if(a[i]>='0' && a[i]<='9')
		{
			printf("%c",a[i]);
		}
	}
	return 0;
}
